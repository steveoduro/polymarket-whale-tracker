# =============================================================================
# Polymarket Whale Tracker & Copy Trading Bot
# =============================================================================

# Environment
NODE_ENV=development

# =============================================================================
# DATABASE (Supabase)
# =============================================================================
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_ANON_KEY=your-anon-key

# =============================================================================
# WHALE TRACKER SETTINGS
# =============================================================================
POLL_INTERVAL_MS=30000
ALERT_MIN_SIZE=100
TRADE_RETENTION_DAYS=7
PORT=3000

# =============================================================================
# TELEGRAM ALERTS
# =============================================================================
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=

# =============================================================================
# COPY TRADING BOT (Phase 2)
# =============================================================================

# Your Polymarket Wallet
WALLET_ADDRESS=0x...your-polygon-wallet-address
WALLET_PRIVATE_KEY=your-private-key-from-metamask

# Trading Settings
COPY_TRADE_SIZE=1.50        # Amount in USDC per copy trade
MIN_WHALE_SIZE=10           # Only copy whale trades >= $10
MAX_POSITION_PER_MARKET=5   # Max total exposure per market
MIN_BALANCE_TO_TRADE=10     # Stop trading if balance drops below $10
DAILY_LOSS_LIMIT=15         # Stop trading if daily loss exceeds $15

# Copier Polling (how often to check for new whale trades)
COPIER_POLL_INTERVAL_MS=10000

# =============================================================================
# WEATHER BOT SETTINGS
# =============================================================================

# Tomorrow.io API Key (optional, for NYC forecast comparison)
# Sign up at https://www.tomorrow.io/ - Free tier: 500 calls/day
TOMORROW_API_KEY=

# Risk Management (Kelly Criterion)
MIN_PROBABILITY=0.20          # Only trade ranges with ≥20% probability
KELLY_FRACTION=0.5            # Half Kelly (conservative) - use 0.25 for quarter Kelly
MAX_POSITION_PERCENT=0.10     # Max 10% of bankroll per position
MIN_BET_SIZE=10               # Minimum $10 per trade

# =============================================================================
# VPS/PROXY SETTINGS (for non-US trading)
# =============================================================================
# If using SOCKS5 proxy, configure in your environment or use SSH tunnel
# Example: ssh -D 1080 -N user@46.62.170.247
# Then set: HTTPS_PROXY=socks5://127.0.0.1:1080
